# Ch12 Getting Ready to Bid

## red: [`1b21fb5`](https://github.com/titangene/goos-ts/commit/1b21fb565ffa29d5edb6d02a4c6d08ddbd9e839b) ［12.2.1 p106］

- 描述：新增 `sniperMakesAHigherBidButLoses` 測試，呼叫 `FakeAuctionServer.reportPrice()`、`ApplicationRunner.hasShownSniperIsBidding()`、`FakeAuctionServer.hasReceivedBid()` 這三個目前都不存在的方法，`npm run typecheck` 出現三個 `TS2339` 錯誤，對應 goos-java 的「won't compile」紅燈
- 對應 goos-java commit：[`ad9b7e5`](https://github.com/titangene/goos-java/commit/ad9b7e5d997a1ce5dc1631223c14fdd8845463eb)
- 相關議題：
  - [`docs/xmpp.md`：XMPP 連線參數](./xmpp.md#xmpp-連線參數)

## red: [`bbe240d`](https://github.com/titangene/goos-ts/commit/bbe240dcdaa84fcecbf693081083997e1edb84c5) ［12.2.2 p107］

- 描述：`FakeAuctionServer` 新增 `reportPrice()`、`hasReceivedBid()`，Smack wrapper 補上訊息 body 的收送能力才能支援這兩個新方法。`npm run typecheck` 的 `TS2339` 錯誤從三個減少為一個：`ApplicationRunner.hasShownSniperIsBidding` 還不存在，測試仍然無法編譯
- 對應 goos-java commit：[`ce2cb2f`](https://github.com/titangene/goos-java/commit/ce2cb2fd28225569708f953509bfe27d3a0afe43)
- 相關議題：
  - [`docs/xmpp.md`：Smack 相容介面封裝](./xmpp.md#smack-相容介面封裝)
  - [`docs/e2e-testing.md`：FakeAuctionServer 訊息比對機制（receivesAMessage）](./e2e-testing.md#fakeauctionserver-訊息比對機制receivesamessage)

## red: [`fdba9f5`](https://github.com/titangene/goos-ts/commit/fdba9f5cfa0513c0d7ac4e462fa8db7e915d4f34) ［12.2.2 p108］

- 描述：`hasReceivedJoinRequestFromSniper()` 泛化成 `hasReceivedJoinRequestFrom(sniperId)`，改成精確比對訊息內容等於 `JOIN_COMMAND` 並檢查寄件人；`Main.ts` 新增 `JOIN_COMMAND`／`bidCommand()` 供 production 與測試共用格式。`npm run typecheck` 仍是同一個 `TS2339` 錯誤：`ApplicationRunner.hasShownSniperIsBidding` 還不存在，測試仍然無法編譯，跟 goos-java 這個 commit 同時列出兩個 Test case 的意圖一致
- 對應 goos-java commit：[`869b44c`](https://github.com/titangene/goos-java/commit/869b44cf7af68b2e14e2a4e4f49a565d2fe95161)
- 相關議題：
  - [`docs/e2e-testing.md`：FakeAuctionServer 訊息比對機制（receivesAMessage）](./e2e-testing.md#fakeauctionserver-訊息比對機制receivesamessage)

## red: [`fbec61d`](https://github.com/titangene/goos-ts/commit/fbec61dbf909a35a0c40e5a98845766a733a17cd) ［12.2.3 p110］

- 描述：`Main.ts` 的 `joinAuction()` 改送出真正的 `JOIN_COMMAND`；`ApplicationRunner` 新增 `STATUS_BIDDING` 常數與 `hasShownSniperIsBidding()`，`npm run typecheck` 沒有錯誤，測試可以編譯並執行。已用 Playwright 實際跑出：`sniperJoinsAuctionUntilAuctionCloses` 轉綠燈，`sniperMakesAHigherBidButLoses` 仍紅燈但原因跟 goos-java 不同——goos-java 這步會在 `XMPPConnection.login()` 撞到 SASL conflict(409)，goos-ts 沒有重現這個現象，仍是卡在等 "Bidding" 逾時（`Main.ts` 還是把任何訊息都當成 "Lost"）
- 對應 goos-java commit：[`f80ef84`](https://github.com/titangene/goos-java/commit/f80ef8420785b04f9ddc54b753d227b1d7cbbb3a)
- 相關議題：
  - [`docs/ui.md`：Sniper 狀態文字（STATUS_BIDDING）](./ui.md#sniper-狀態文字status_bidding)
  - [`docs/e2e-testing.md`：ApplicationRunner 的 server 生命週期管理](./e2e-testing.md#applicationrunner-的-server-生命週期管理)
